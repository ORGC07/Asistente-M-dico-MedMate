import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.grupo16.medmate',
  appName: 'MedMate',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
